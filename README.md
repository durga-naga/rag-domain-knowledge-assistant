# rag-domain-knowledge-assistant
An AI-powered Domain Knowledge Assistant implementing Retrieval-Augmented Generation (RAG). It extracts knowledge from PDFs, performs semantic search using FAISS embeddings, and generates context-aware answers using Groq LLM via Streamlit interface.
